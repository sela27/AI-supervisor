# Secrets — never committed
.env
.env.*
!.env.example

# What one deployment is. Copied from the example beside it and filled in.
supervisor.config.json

# Where a container deployment keeps the Claude credentials its nights run on,
# unless it was pointed somewhere else.
/claude/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local state
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite3
data/

# Node
node_modules/
dist/
*.tsbuildinfo

# OS / editors
.DS_Store
Thumbs.db
